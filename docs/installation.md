# Installation

SecretGuard requires Python 3.9 or newer.

```bash
git clone https://github.com/example/secretguard.git
cd secretguard
pip install .
```

For development:

```bash
pip install -r requirements.txt
pip install -e .
pytest
```
